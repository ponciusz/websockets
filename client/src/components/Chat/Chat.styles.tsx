import styled from 'styled-components';

const Wrapper = styled.div`
  /* background-color: red; */
`;

const ChatBox = styled.div`
  /* background: green; */
  box-shadow: 0 0 0 2px gray;
  height: 500px;
`;

export default { Wrapper, ChatBox };
